name: Oasis Windows Build

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      # Your project lives inside the subfolder: codeine-dreams/
      - name: Install dependencies
        run: npm install
        working-directory: codeine-dreams

      - name: Build project
        run: npm run build
        working-directory: codeine-dreams

      - name: Verify build output
        run: |
          if (!(Test-Path "codeine-dreams/build/windows")) {
            echo "❌ Build folder missing: codeine-dreams/build/windows"
            exit 1
          } else {
            echo "✅ Build folder found."
            dir codeine-dreams/build/windows
          }

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: oasis-build-win
          path: codeine-dreams/build/windows/

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: oasis-build-win
          path: ./artifact

      - name: List artifact contents
        run: ls -R ./artifact

      - name: Deploy
        uses: actions/deploy-pages@v4
